Installation using source package (command line instructions)

1. unpack bambam-0.3 to create the bambam-0.3 directory
	tar zxvf bambam-0.3.tar.gz
2. move into the bambam directory
	cd bambam-0.3
3. execute 
	./bambam.py
4. to exit, type quit
